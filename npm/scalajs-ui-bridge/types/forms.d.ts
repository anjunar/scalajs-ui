/** Installs only the forms registrations (form, sub-form, input, field-set, array-form, combo-box, image-cropper). */
export { bridgeRuntime, parseLocalDate, parseInstant, parseLocalDateTime } from "./index.js";
export type { ScalaLocalDate, ScalaInstant, ScalaLocalDateTime } from "./index.js";
